{
  "image": {{#if image}}"{{image}}"{{else}}null{{/if}},
  "slug": {{#if slug}}"{{slug}}"{{else}}null{{/if}},
  "name": {{#if name}}"{{name}}"{{else}}null{{/if}},
  "description": {{#if desciption}}"{{desciption}}"{{else}}null{{/if}}
}